eureka:
  client:
    service-url:
      dufaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: product
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      profile: dev
#server:
#  port: 8763